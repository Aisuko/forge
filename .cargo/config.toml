# getrandom 0.3 needs an explicit backend opt-in on wasm32-unknown-unknown
# (used via `rand`; see the `wasm_js` feature in Cargo.toml).
[target.wasm32-unknown-unknown]
rustflags = ['--cfg', 'getrandom_backend="wasm_js"']
